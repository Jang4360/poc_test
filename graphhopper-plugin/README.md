# GraphHopper Plugin Module

This module reserves the Java ownership boundary for custom GraphHopper encoded values and tag parsers.

Stage 01 adds the build scaffold only. Workstream 04 will populate the module with the actual SPI implementation that reads `road_segments` in bulk and injects accessibility encoded values during import.
